[phases.setup]
nixPkgs = ["python311", "gcc", "binutils"]

[phases.install]
cmds = [
  "pip install --no-cache-dir --upgrade pip setuptools wheel",
  "pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = "python3 bot.py"
